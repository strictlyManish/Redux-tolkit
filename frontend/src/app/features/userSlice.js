import { createSlice } from '@reduxjs/toolkit'



const userSlice = createSlice({
    name:'user',
    initialState:{
        value:[]
    },
    reducers:{
        registerUser : 
        registerUser : 
    }
});

export default userSlice